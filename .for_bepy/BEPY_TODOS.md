# BEPY TODOs

### Visual QA

- Play a full game on two real phones over LAN via the new lobby: with the dev stack running (`pnpm dev:all`), open the screen at `http://<this-PC-LAN-ip>:5173` and scan the QR (or visit `http://<LAN-ip>:5174` on two phones). On each phone set an identity (name + color + emoji - saved once), enter the room code, and join. The first phone to join is the host: it shows a D-pad + game tiles. Host picks a game (Tic-Tac-Toe or Ultimate TTT), play it, then host taps "Back to lobby" and picks the other game. Confirm moves/turn-locking/win all sync to the screen and that the lobby keeps both players across game switches. (Claude verified the entire flow browser-to-browser via Playwright; only the actual-phone hop + touch needs your devices. LAN IPs seen during dev: 192.168.178.67, 100.74.99.126.)
- Phase 3 Electron host - dev run: from the repo root run `pnpm host:dev`; an Electron window should open showing the lobby (room code + QR). Scan the QR from a phone on the same WiFi (it points at `http://<LAN-ip>:7780/?room=<CODE>`), join from two phones, set identities, and have the host launch + play a game. NOTE: the host now serves BOTH games via the lobby (it no longer boots straight into one game). (Claude unit/integration-tested every server-side piece - LAN-IP detection, both static servers, the WS join round-trip - but the Electron window needs a display and real phones need your devices.)
- Phase 3 portable build: run `pnpm host:package`, then double-click `apps/host-desktop/release/Hubbub-portable.exe` (no install). Confirm the lobby window opens and phones can join + play via the lobby. Then send that single .exe to a friend on your network and confirm they can host a game with zero install. (Claude confirmed the .exe builds; only the run + send-to-friend hop needs you.)
