# Tomorrow's AI Prompt

Generated by: manual (/next-ai-prompt)
Generated: 2026-06-25 02:45 UTC
Branch: master

## Context

Shipped the full AirConsole-style multi-game lobby (spec + plan + 7 implementation tasks,
commits `50a824e`..`1261ba7`). Players join a persistent gang once with a saved identity
(name + color + emoji in localStorage); the first joiner is host and picks/switches games
from a screen-rendered, controller-navigable picker (D-pad + tiles); the server hosts a game
registry and swaps the active GameInstance at runtime keeping the room + players. Floor green
(9 typecheck, 72 tests, 4 builds) and the full loop verified live via Playwright. The
`HUBBUB_GAME` / `VITE_GAME` env switches are now RETIRED - games are chosen in the lobby.

## Verify checklist

- [ ] `git pull` (local-only repo, no remote - just confirm you are on `master` at `1261ba7`)
- [ ] `pnpm install` then `pnpm typecheck` (expect 9 ok) and `pnpm test` (expect 72 tests, 15 files)
- [ ] `pnpm build` (expect 4 ok)
- [ ] Live: `pnpm dev:all`, open the screen (`http://localhost:5173`), open two controller tabs at `http://localhost:5174` (clear localStorage between them - same-origin reconnect-token collision), set two identities, host launches a game from the lobby, plays a move, returns to lobby, launches the other game
- [ ] Confirm no `VITE_GAME`/`HUBBUB_GAME` env is needed anywhere anymore

## Open decisions

- Lobby visual polish is intentionally minimal (functional landing page). Joe may want a [UX] pass on the carousel/tiles/roster styling before it feels "real".
- Whether to build game suggestions next (non-host taps to suggest) vs mid-game join vs a third game (Music Guesser / Deezer) vs Phase 4 real-time. See "Suggested next steps".

## Files changed

- `packages/protocol/src/messages.ts` (+test) - identity fields, lobby client messages, reshaped roomState, gameState gains gameId
- `packages/sdk/src/registry.ts` (+test), `packages/sdk/src/index.ts` - GameRegistry type + gameSummaries helper
- `apps/server/src/rooms.ts` (+test) - host/mode/cursor/identity, host migration + transfer
- `apps/server/src/server.ts`, `index.ts`, `server.test.ts`, `server.lobby.test.ts` (new, replaced server.game.test.ts), `server.uttt.test.ts` - registry + runtime lobby switching
- `apps/screen/src/lobby.tsx` (new), `game.tsx`, `App.tsx` - landing-page lobby + mode routing + dynamic registry
- `apps/controller/src/identity.ts` (new), `settings.tsx` (new), `lobby.tsx` (new), `game.tsx`, `App.tsx` - identity-first settings, host D-pad lobby, in-game + back-to-lobby
- `apps/host-desktop/src/host.ts` (+test), `package.json` - serves the full game registry
- Docs: `docs/superpowers/specs/2026-06-25-multi-game-lobby-design.md`, `docs/superpowers/plans/2026-06-25-multi-game-lobby.md`, `docs/PROJECT_STATE_AND_NEXT_STEPS.md` (run note updated)

## Suggested next steps

- Pick the next build target: (a) game suggestions from non-host phones, (b) mid-game join (forward-compatible via `onPlayersChanged`; design notes in the spec), (c) a third game like Music Guesser (Deezer audio, never Spotify), or (d) Phase 4 real-time (`tickRateHz` + screen loop; needs the LAN-HTTPS/secure-context decision first).
- Optional [UX] polish pass on the lobby (carousel/tiles/roster, active-board glow, small-phone sizing).
- Wire `HUBBUB_GAME` is gone; if desired, add a dev convenience to auto-pick a game on boot (low priority - the lobby is the selector now).
- The three dev servers (screen 5173, controller 5174, ws 7787) may still be running under the supervisor from the last session; restart or reuse as needed.
