# Tomorrow's AI Prompt

Generated by: manual (/next-ai-prompt, --mode manual)
Generated: 2026-06-24 10:21 UTC
Branch: master

## Context

Hubbub (open-source AirConsole-style party-game framework) is through **Phase 0 (foundation + bare room)** and **Phase 1 (game SDK + first game)**. A pnpm/Turborepo TS monorepo with `@hubbub/protocol` (Zod messages + swappable `ClientTransport` + reconnect tokens), a `ws` room server, `@hubbub/sdk` (the `GameLogic` contract + `GameInstance` runtime + React helpers), and a playable `@hubbub/game-tictactoe` rendered by the `screen` + `controller` apps. Verified end-to-end via Playwright: two players join as X/O, play a full game, win detection + game-over lockout sync to the screen. **27 tests pass, 6 packages typecheck, both apps build.** Nothing is deployed; local dev only.

## Verify checklist

- [ ] `git pull` (note: local-only repo, no remote yet, skip if unchanged)
- [ ] `pnpm install` then `pnpm typecheck` (expect 6 ok) and `pnpm test` (expect 27 passing)
- [ ] Restart the stack via `/supervised-run` (process id `hubbub:dev-all`, cmd `pnpm dev:all`); confirm screen `:5173`, controller `:5174`, server ws `:7787`
- [ ] Open `http://localhost:5173`, join from two isolated controller tabs (clear `localStorage` in the 2nd so it becomes a distinct player), play a game, confirm screen + controllers stay in sync
- [ ] After any code task: run `/commit` yourself (subagents must NOT commit)

## Open decisions

- **What to build next:** Phase 3 (Electron host = sendable local product) vs add Ultimate Tic-Tac-Toe (second game on the proven contract) vs Phase 5 (cloud). Joe leaned toward Electron/Ultimate; confirm.
- **Product name** still the codename `hubbub` (decide before any public release). MIT `LICENSE` copyright holder is the placeholder `Joe`.
- **Two-tab test limitation:** controllers persist a reconnect token in `localStorage` keyed by room code, so two tabs in one browser collide; the e2e clears storage in the 2nd tab. Real two-phone play is the open BEPY todo.

## Files changed

- `packages/protocol/*` — messages (incl. `action`/`gameState`), `ClientTransport` + WS impl, token/code helpers (token helpers on a `/tokens` subpath to keep the index browser-safe)
- `packages/sdk/*` — `GameLogic` contract, `GameInstance` runtime, `/react` helpers (`useGameState`, `createActionSender`)
- `packages/games/tictactoe/*` — pure logic + `/screen` + `/controller` (subpath-split so the server never imports React)
- `apps/server/*` — `RoomManager`, `ws` server, hosts a per-room game, broadcasts `gameState`
- `apps/screen/*`, `apps/controller/*` — render the game; default server port is **7787** (8787 is taken on this machine)
- `docs/superpowers/specs/*`, `docs/superpowers/plans/*` — design spec + Phase 0 & Phase 1 plans
- root: `pnpm-workspace.yaml` (incl. `packages/games/*`), `turbo.json`, `vitest.config.ts`, `tsconfig.base.json`, `LICENSE`, `CLAUDE.md`

## Suggested next steps

- **RUN AS AN ORCHESTRATOR (Joe's explicit request to conserve context).** Use `superpowers:subagent-driven-development`: write/load the phase plan, then dispatch ONE subagent per task. Each subagent prompt MUST end with: "Stage your changes but do NOT commit. The main agent will run `/commit` after your report-back." Subagents return a short report (pass/fail + staged file list), not file dumps — that is what keeps your context small.
- **Keep these in the lead session (subagents can't):** running `/commit` after each task, bringing the stack up via `/supervised-run`, and the Playwright e2e verification (this browser work is the biggest context cost — keep it lean; consider delegating only the `pnpm test` runs).
- **Likely next phase = Phase 3 (Electron host):** an Electron app bundling the Node server + screen so Joe can send a friend a double-click app and play TTT on real phones over LAN. See the spec's build order. Write a Phase 3 plan first (writing-plans), then orchestrate it.
- **Alternative quick win:** Ultimate Tic-Tac-Toe as a second game — pure logic on the existing contract, great parallelizable subagent task.
- Read `docs/superpowers/specs/2026-06-24-hubbub-party-game-framework-design.md` and `CLAUDE.md` (architecture invariants) before starting.
